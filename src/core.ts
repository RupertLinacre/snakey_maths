import { START_LIVES } from './constants';
import type { State } from './types';

export function initState(): State {
  throw new Error('initState is not implemented yet. START_LIVES=' + START_LIVES);
}
